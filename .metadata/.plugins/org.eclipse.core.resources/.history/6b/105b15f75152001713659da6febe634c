import com.google.inject.Guice;
import com.google.inject.Inject;
import com.google.inject.Injector;

public class MyAppilcation {
	
	public static void main(String[] args) {
		Injector injector = Guice.createInjector().getInstance(Greater.class);
		//Greater greater = injector.getInstance(Greater.class);
		greater.sayHello();
	}
}
