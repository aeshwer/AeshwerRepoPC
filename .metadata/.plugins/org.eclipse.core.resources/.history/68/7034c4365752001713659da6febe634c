package LearnGuiceCalculatorModify;

import com.google.inject.AbstractModule;

public class CalculatorModule extends AbstractModule{

	@Override
	protected void configure() {
		bind(ICalculator.class).to(Calculator.class);
	}

}
